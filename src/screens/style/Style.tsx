import { StyleSheet } from 'react-native';

const SwellStyle = StyleSheet.create({
    container: {
        backgroundColor: 'grey',
      margin: '3%',
    },

    title:{
        alignItems: 'center',
        justifyContent: 'center',
    }
});

export default SwellStyle;